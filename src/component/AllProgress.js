import React from 'react';
import Navigation from './footer';

const AllProgress = () => (
  <div>
    Is responsible to display all results
    <Navigation />
  </div>

);
export default AllProgress;
